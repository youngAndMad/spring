create table if not exists car (
                     id serial primary key,
                     brand varchar(255),
                     model varchar(255),
                     made_year integer
);